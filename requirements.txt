# Google Colab already provides a mutually compatible torch, torchvision,
# torchaudio, CUDA and cuDNN stack. Do not reinstall those packages here.
pytorch-lightning>=2.2,<3
torchmetrics>=1.3,<2
tensorboard>=2.15,<3

numpy>=1.26,<3
Pillow>=10,<12
tqdm>=4.66,<5
regex>=2023.10.3
ftfy>=6.1,<7
packaging>=23.2
